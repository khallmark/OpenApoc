Put your copy of the game here as data/cd.iso (ISO file or extracted directory).

For this checkout, symlink the Steam ISO instead of copying it:

  ln -sfn "$PWD/depot_7661/cd.iso" data/cd.iso

See docs/local-development.md for the full setup.
