# Next implementation targets

Written from [openapoc-gap-matrix.md](openapoc-gap-matrix.md). Ghidra and decompiler output are allowed on this fork.

A row is not done until the lock test fails without the change. Remaining items have **no recovered constant or consumer** — do not invent one.

Last lock: building detection weights `0x155354` (49×uint32; Senate index 1 = 155) and alien infiltration det/move `0x142374` / `0x1423B0` (13×uint32, egg…micronoid). `FUN_000abf9c` @ VA `0xABF9C` / file `0xFE640` (caller `FUN_000aac88` @ file `0xFD32C`) sets the UFOPaedia start byte from `research_data` +25/+26; catalog-bound pages ignore shuffled patch deps (`test_city_rules` `ufopaedia_abf9c_unlock`). Extra cat 3 `0x21→0x22` / `0x1B→0x13+0x17`. Type-mismatched PCX stays unbound. Manufacture records 38–42 keep `()` in the topic ID (`MANUFACTURE_DISRUPTOR_ARMOR_(LEGS)`); patch deletes leftover `__LEGS_` keys (`manufacture_disruptor_armor_ids`). `FUN_0008c860` @ file `0xEEF04` (4-build `0xEEFD4`) type 2/3 hide `DAT_00183b3b` / `DAT_00183b0a`. `FUN_000811fc` @ file `0xE3971` (4-build `0xE39BC`) is the same aequip hide for the shop — week 0 is not a hide bit (AG 19/23, destab 37). `FUN_00073750` @ file `0xD5DF4` and `FUN_0007679c` @ file `0xD8E40` are vequip screen/info-card hide graphics, not a shop list. `FUN_00081610` @ file `0xE3CB4` is an aequip layout pass, not craft-ammo shop. `FUN_00086054` @ file `0xE86F8`, `FUN_0008cb9c` @ file `0xEF240`, and `FUN_0008f7f4` @ file `0xF1E98` are org-base buy/stock/layout UI on the same `0x57` / `DAT_00183b3b` (and type-3 `DAT_00183b0a`) hide RAM — not new shop filters. Do not copy aequip week-0 shop onto vammo / vehicle types. unknown2==4 is 29 rows (`XOR EBX` then `de2b0[topic]` override); leftover Containment `prereqTech[0]=45` is not a combinator input. Overlay `INC` is `FUN_0007b3dc` @ TACP file `0xD5E80` on the tile overlay byte at `0xF3738` — do not invent placement seed 10, map `age`/`power`, or INC on an invented cadence. `FUN_00081774` @ file `0xE3E18` IDIVs live `[+0xF8]` by `ammo_rounds` — numerator unbound. `FUN_000b32ac` @ file `0x115950` is a 4-way event dispatcher, not a cargo-seize entry; do not wire `Cargo::seize` from `worth×50`. TACP `unused01` @ `+0xF` feeds a type-4 doodad (`FUN_000598d4` @ `0xB4378` / `FUN_00058e84` @ `0xB3928`) — visual slot, no damage reader. `+0x168` / `0x1439E0` rebuild / `rebuilding_rate` stay unbound. `leadsTo` is not a research-graph edge. Do not delete Containment’s All(Quantum Lab). Weekly item stock/price stays Wong’s Guide — `FUN_000941dc` @ file `0xF6880` is org budget fractions, not `EconomyInfo::update`. Panic→Stun fall-through in `battleunit.cpp` stays; no TACP listing.

1. **Battle cover-tile / potshot ([issue 265](https://github.com/OpenApoc/OpenApoc/issues/265))** — no printable `cover` / `potshot`. `getTakeCoverMovement` stays null until a TACP cover metric exists.
   - Lock: `test_tactical_ai_retreat` (retreat + panic run only).
   - Pass: Cautious/Normal take cover from a recovered metric.
2. **Enzyme / fire / cloak ticks** — fire item resist (`FUN_0007c110`), type-2 overlay decoding (`FUN_0007ad94`), the 27-byte power lookup (`FUN_0007ae18`), preplaced marker 25 → overlay `0x99`, and stage/extinction/terrain-threshold behavior (`FUN_0007b3dc`) are bound. `Battle::updateFireScheduler` implements and serializes real-time `FUN_0007b7f8`: consume one vanilla iteration per four OpenApoc ticks; process `(mapY×mapZ)/0x48` complete X rows using persistent Y/Z cursors; then check/reset/run/increment the `0x24` item-contact counter. Turn-based organisation-cycle wrap runs `FUN_000b8c50`'s 400-iteration batch, with item contacts but no unit contacts. Recovered overlays skip legacy per-hazard/per-owner progression and clear without invented smoke. Unit fire intensity, generic placement/spread RNG `FUN_0001eee8` / `FUN_0007b0d0`, enzyme, and cloak timing remain unbound; generic hazards and `HAZARD_SPREAD_CHANCE` retain the legacy path.
   - Lock: `test_battle_hazard` (`fire_hazard_item_resist`, `fire_overlay_power_progression`, `fire_overlay_terrain_threshold`, `fire_scheduler_state_machine`), `test_city_rules` (`aequip_artifact_and_resist`, `fire_hazard_power_table`).
   - Pass: bind unit intensity and neighbour selection/RNG before replacing generic fire placement/spread.
3. **Vehicle attack-mode dodge 100/80/50/10** — `loftemps_index` is loftemps, not a percent. `Evade Fire` xrefs empty.
   - Lock: none until an engagement table exists.
   - Pass: do not map 119–151 onto percents.
4. **City Action music ([issue 618](https://github.com/OpenApoc/OpenApoc/issues/618))** — `Action music` / `GROUP_*` catalog-only.
   - Lock: none.
   - Pass: city mix only after a bound consumer.
5. **Org bribe/rift dollar formulas ([issue 996](https://github.com/OpenApoc/OpenApoc/issues/996))** — directed relationship table is extracted; coefficients unbound. `Cargo::seize` relation FIXME is the same class: `FUN_000b32ac` @ file `0x115950` recovers `worth×50` + tiered extra into org `+8` / `FUN_0005faf0`, but `[0x174024]` event type is not bound to seizure.
   - Lock: `test_diplomacy`, `test_organisation`.
   - Pass: do not invent a weekly-drift or seize-diplomacy formula.
6. **Dead gadgets** — Mind Shield +30/cap 200 locked. MultiTracker / VortexAnalyzer / Disruptor `useItem` unbound.
   - Lock: `test_battle_use_item`.
   - Pass: no invented scan/UI formula.
7. **UFO_mission_patterns** — extracted into `base_gamestate`. Do not XInclude a patch copy (serialize appends `missionList`). Bound xrefs to VA `0x128D64` are empty; IDs 3/1/2/5 are campaign first-appearance (weeks 1/4/5/7), not a recovered `FUN_*`.
   - Lock: `test_city_rules` (`ufo_mission_preference_loaded`).
8. **Subversion / X-COM base exposure** — the invented `micronoidRainChance` / `tryMicronoidRain` takeover path is removed. `+0x2BC` is the terminal word of each 0x2BE-byte, 16-entry X-COM Base runtime record, keyed by base building ID; OpenApoc serializes it as `Base::knownToAliens`. Role-2 arrival (`FUN_0003a910`) uses doodad 11 then `FUN_0007062c` @ object-page `0x6062B` to mark an unexposed base; an exposed-base follow-up uses smoke/infiltration doodads 13/14 and the normal alien-deposit completion path. Normal UFO deposit (`FUN_0005fddc` @ `0x4FDDB`) exposes a base when inclusive `rand16(100)<5`; alien movement (`FUN_0006f7f8` @ `0x5F7F7`) uses `< moved_count×5`. `FUN_000702e4` targeting is also implemented: inclusive `rand16(15)` start, 16-slot cyclic scan, first active exposed base, and exactly one preferred target per incursion; later craft retain the existing fallback. Hourly organisation takeover remains separate in `FUN_0007fcc0` @ file `0xD2364`.
   - Lock: `test_vehicle_mission` (`subversion_completion_exposes_base_without_takeover`, `alien_exposure_threshold`, `known_base_slot_selection`), `test_city_rules` (`subversion_prefers_one_known_base`); `test_organisation` locks hourly takeover behavior.
   - Pass: bind global override `DAT_000e0cc0` and event-dispatch full-transfer writer `FUN_0006f738`; do not restore an arrival percentage.
9. **`UFO_mission_data` tail leftovers** — `follow_slot`, spawn XY, `type_percent` constitution multiply, the scatter>50 clamp, and +0x1B are consumed. Hexa's `building_function` label for +0x1B was wrong: `FUN_0006da88` @ file `0xD030B` copies it to vehicle +0x171, and `FUN_0003a910` @ object-page file `0x2A90F` decrements it whenever the UFO reaches a mission destination; zero advances target/mission state. It is extracted as `mission_counter` (A1 proves values 12 exceed the ten gate slots). VA `0x1439E0` is dimension-gate runtime RAM, not an org table: `FUN_0006cdd0` @ object-page file `0x5CDCF` initializes it, `FUN_0006eab0` @ `0x5EAAF` creates paired gates between dimensions, and `FUN_000706f8` @ `0x606F7` rebuilds ten human-city gate positions. `FUN_0005e7f4` @ `0x4E7F3` tests whether an xyz coordinate lies inside any live vehicle volume in the current dimension; it is not a tile probe. `FUN_000588f8` @ object-page file `0x488F7` gates +0x168 against constitution +0x12e and remains unbound. The exact OpenApoc mission-counter transition is also not yet implemented.
   - Lock: `test_city_rules` (`ufo_incursion_table`, `ufo_incursion_spawn_xy`, `ufo_incursion_follow_type`).
   - Pass: bind the +0x171 counter transition and `FUN_000588f8` / `FUN_0004dd14`; do not add a building-function filter.
10. **Base-exposure leftovers** — target preference is consumed without replacing the decompiler-damaged fallback. `FUN_0006f738` also writes exposure during event-dispatch population transfer using per-species `moved_count×5`, but event types 1/4 are not mapped. `DAT_000e0cc0` makes all active bases pass the exposure predicate; its campaign lifecycle remains unbound.
   - Lock: targeting and direct/movement exposure locks are listed above.
   - Pass: identify the override lifecycle and event types 1/4 before adding those writers.

Extractor CRC: [exe_slide.h](../../tools/extractors/common/exe_slide.h) maps UFO2P `0xdbd3b41d` → `+0xE00` (except `crew_ufo_downed`, P↔P4 same file offset) and TACP `0x3ec9c268` → `−0x2200`. Unknown CRCs log and keep slide 0. Lock: `test_research` `exe_slide_crcs`.

Do not put extracted list tables under `data/common_patch/gamestate/` — `loadGame` appends vectors and doubles fleet/spawn counts. Reference dumps live in [exe-tables](exe-tables/).
