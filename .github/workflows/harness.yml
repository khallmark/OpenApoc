name: Harness

on: [push, pull_request]

jobs:
  ignored-binaries:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Confirm depot, ISO, and Ghidra DBs stay ignored
        run: ./tools/check_ignored_binaries.sh
