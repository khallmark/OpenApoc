name: Lint

on: [push, pull_request]

permissions:
  contents: read

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release
  CLANG_FORMAT: clang-format-18
  CLANG_TIDY: clang-tidy-18
  BUILD_DIRECTORY: ${{ github.workspace }}/../build

jobs:
  build:
    # The CMake configure and build commands are platform agnostic and should work equally
    # well on Windows or Mac.  You can convert this to a matrix build if you need
    # cross-platform coverage.
    # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        submodules: recursive
        fetch-depth: 0
        persist-credentials: false

    - name: Create Build Environment
      # Some projects don't allow in-source building, so create a separate build directory
      # We'll use this as our working directory for all subsequent commands
      run: cmake -E make_directory "$BUILD_DIRECTORY"

    - name: Download minimal cd image
      run: |
        curl --fail --show-error --location --proto '=http' --retry 3 \
          http://s2.jonnyh.net/pub/cd_minimal.iso.xz \
          --output "$GITHUB_WORKSPACE/data/cd.iso.xz"
        printf '%s  %s\n' \
          '4e0e93a4110c2e52778d1468b96d02bd062324121773c345913ebf6852000631' \
          "$GITHUB_WORKSPACE/data/cd.iso.xz" | sha256sum --check --strict
    
    - name: Extract minimal cd image
      run: xz -d "$GITHUB_WORKSPACE/data/cd.iso.xz"

    - name: Update apt-get repo
      run: sudo apt-get update

    - name: Install dependencies
      run: sudo apt-get install libsdl2-dev cmake build-essential git libunwind8-dev libboost-locale-dev libboost-filesystem-dev libboost-program-options-dev qt6-base-dev libvorbis-dev ninja-build "$CLANG_FORMAT" "$CLANG_TIDY"

    - name: Configure CMake
      # Use a bash shell so we can use the same syntax for environment variable
      # access regardless of the host operating system
      shell: bash
      working-directory: ${{ env.BUILD_DIRECTORY }}
      # Note the current convention is to use the -S and -B options here to specify source 
      # and build directories, but this is only available with CMake 3.13 and higher.  
      # The CMake binaries on the Github Actions machines are (as of this writing) 3.12
      run: cmake "$GITHUB_WORKSPACE" -DCMAKE_BUILD_TYPE="$BUILD_TYPE" -GNinja -DCLANG_FORMAT="$CLANG_FORMAT" -DCLANG_TIDY="$CLANG_TIDY"

    - name: Setup Environment (PR)  
      if: ${{ github.event_name == 'pull_request' }}  
      shell: bash  
      run: |
        printf '%s\n' "AFTER_COMMIT_SHA=${{ github.event.pull_request.head.sha }}" >> "$GITHUB_ENV"
        printf '%s\n' "BEFORE_COMMIT_SHA=${{ github.event.pull_request.base.sha }}" >> "$GITHUB_ENV"

    - name: Setup Environment (Push)  
      if: ${{ github.event_name == 'push' }}  
      shell: bash  
      run: |
        printf '%s\n' "AFTER_COMMIT_SHA=${{ github.event.after }}" >> "$GITHUB_ENV"
        printf '%s\n' "BEFORE_COMMIT_SHA=${{ github.event.before }}" >> "$GITHUB_ENV"

    - name: format lint
      working-directory: ${{ github.workspace }}
      shell: bash
      run: |
        cmake --build "$BUILD_DIRECTORY" -t format-sources
        if [[ -n "$(git -C "$GITHUB_WORKSPACE" status --porcelain)" ]]; then
          echo 'Format mismatch:'
          git -C "$GITHUB_WORKSPACE" diff
          exit 1;
        fi
        

    - name: clang-tidy lint
      working-directory: ${{ github.workspace }}
      shell: bash
      run: |
        if [[ "$BEFORE_COMMIT_SHA" == "0000000000000000000000000000000000000000" ]]
        then
          BUILD_DIR="$BUILD_DIRECTORY" "$GITHUB_WORKSPACE/tools/lint-tidy.sh" "$AFTER_COMMIT_SHA" || true
        else
          BUILD_DIR="$BUILD_DIRECTORY" "$GITHUB_WORKSPACE/tools/lint-tidy.sh" "$BEFORE_COMMIT_SHA..$AFTER_COMMIT_SHA" || true
        fi
        
