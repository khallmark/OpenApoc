trunk_commits: allowed

This is a personal fork (origin: khallmark/OpenApoc). `develop` here is a working
branch, not an upstream trunk — upstream is the separate `OpenApoc` remote, and
that is the only history worth guarding. Committing on `develop` is normal
practice in this checkout.
